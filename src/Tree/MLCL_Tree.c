/*
 *   This file is part of the MLCL Library.
 *   Antoine Bastos 2022
 *
 *   This Library is free software under the terms of
 *   the MIT license.
 */

#include "../../include/Tree/MLCL_Tree.h"

/* Tree = new_tree(&x, TreeTypes.BST);
 * tree_add(t, &x);
 *
 * avl inherit from bst so
 * Tree = new_tree(&x, TreeTypes.AVL);
 * typedef struct s_avl_node_ {
    void * data;
    int balance;
    struct s_tree_node *left, *right;
    struct s_avl_tree_descriptor * d;
} AVLTreeNode, * AVLTree;
 * */
